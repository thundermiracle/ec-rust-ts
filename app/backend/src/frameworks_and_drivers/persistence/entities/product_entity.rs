#[allow(dead_code)]
pub struct ProductEntity {
    pub id: u32,
    pub name: String,
    pub price: u32,
    pub description: String,
    pub quantity: u32,
    pub created_at: String,
    pub updated_at: String,
}